# jorge_gutierrez_exercise

A new Flutter project.

## Getting Started

You need to run the following commands to generate the required files

```
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```